apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hapitest-api.fullname" . }}-test-connection"
  labels:
    {{- include "hapitest-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hapitest-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
