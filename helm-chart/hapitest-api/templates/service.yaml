apiVersion: v1
kind: Service
metadata:
  name: {{ include "hapitest-api.fullname" . }}
  labels:
    {{- include "hapitest-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hapitest-api.selectorLabels" . | nindent 4 }}
